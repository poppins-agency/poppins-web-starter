// import { fetchSBPageData } from "@/queries/baseQuery";

export default async function Home() {
  // const { data } = await fetchSBPageData("home");
  return <div>{/* <h1>Story: {data.story.name}</h1> */}</div>;
}
